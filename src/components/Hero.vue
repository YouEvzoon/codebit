<template>
  <section id="inicio" class="hero"><div class="hero__grid"></div><div class="container hero__content"><p class="eyebrow">Páginas web profesionales</p><h1>Tu negocio<br><em>merece</em> crecer.</h1><p class="hero__text">Creamos páginas web modernas, rápidas y adaptadas a celulares para que más clientes encuentren tu negocio.</p><div class="hero__actions"><a class="button" href="#precios">Ver oferta Q1,500 <span>↗</span></a><a class="hero__scroll" href="#servicios">Qué incluye <span>↓</span></a></div></div><div class="hero__stamp">VUE.JS<br><span>RÁPIDA — SEGURA — RESPONSIVE</span></div></section>
</template>
<style scoped>
.hero { position: relative; min-height: 760px; display: flex; align-items: center; overflow: hidden; background: radial-gradient(circle at 77% 43%, rgba(57,215,119,.14), transparent 25%), var(--ink); }.hero__grid { position: absolute; inset: 0; opacity: .16; background-image: linear-gradient(var(--line) 1px, transparent 1px), linear-gradient(90deg, var(--line) 1px, transparent 1px); background-size: 80px 80px; mask-image: linear-gradient(90deg, transparent, black 55%, transparent); }.hero__content { position: relative; z-index: 1; padding-top: 150px; }.hero h1 { max-width: 800px; margin: 26px 0 28px; font-size: clamp(64px, 10vw, 142px); letter-spacing: -7px; }.hero h1 em { color: var(--acid); font-style: normal; }.hero__text { max-width: 390px; color: var(--muted); line-height: 1.6; }.hero__actions { display: flex; align-items: center; gap: 30px; margin-top: 38px; }.hero__scroll { color: var(--muted); font-size: 13px; }.hero__scroll span { margin-left: 10px; color: var(--acid); }.hero__stamp { position: absolute; right: 6%; bottom: 11%; color: var(--acid); font-size: 20px; line-height: 1.1; transform: rotate(-8deg); }.hero__stamp span { color: var(--muted); font-size: 8px; letter-spacing: 1px; }
@media (max-width: 700px) { .hero { min-height: 760px; align-items: flex-start; }.hero__content { padding-top: 150px; }.hero h1 { letter-spacing: -3px; }.hero__stamp { right: 7%; bottom: 7%; } }
</style>